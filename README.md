# Playwright Books Scraper

This project is a web scraper for books using Playwright.

## Structure
- `scraper.py`: Main scraping script
- `output/books.json`: scraped data output

## Usage
1. Install dependencies: `pip install -r requirements.txt`
2. Run scraper: `python scraper.py`
